package gr.uoa.di.thanos.botcraft.renderers;

/**
 * Enumeration representing texture coordinates components.
 * 
 * @author thanos
 */
public enum TextureCoordinatesComponent {
	/**
	 * u (texture coordinates component along the horizontal axis).
	 */
	U,

	/**
	 * v (texture coordinates component along the vertical axis).
	 */
	V
}
